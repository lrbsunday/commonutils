.. highlight:: shell

============
Installation
============


Stable release
--------------

To install commonutils, run this command in your terminal:

.. code-block:: console

    $ pip install commonutils

This is the preferred method to install commonutils, as it will always install the most recent stable release. 

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for commonutils can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/lrbsunday/commonutils

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/lrbsunday/commonutils/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/lrbsunday/commonutils
.. _tarball: https://github.com/lrbsunday/commonutils/tarball/master
